[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.110", "uvicorn>=0.27", "anthropic>=0.30,<1.0", "pydantic>=2.0", "python-dotenv>=1.0", "opentrons>=7.0", "cryptography>=42.0" ]
requires-python = "~=3.12.0"
